# 夹具管理系统使用说明 — v2.33

## 快速入门

### 1. 新建夹具

1. 点击左侧菜单「🔩 夹具管理」
2. 点击「➕ 新建夹具」按钮
3. 填写：
   - **编号**：4位纯数字（如 2001）
   - **名称**：夹具名称（必填）
   - **规格型号**：可选
   - **分类**：如 冲压/焊接/装配
   - **库位**：可选，可复用备件库位
4. 点击「建档」

### 2. 打印二维码标签

夹具建档后自动生成二维码（SPMS1|2001），可打印标签贴在夹具上。

### 3. 夹具出库

1. 在夹具列表中找到要出库的夹具
2. 点击「出库」按钮
3. 填写：
   - **经办人**（必填）
   - **领用人/使用部门**
   - **出库时间**（默认当前时间）
   - **备注**（可选）
4. 点击「确认出库」

**状态变化**：在库 → 已出库  
**计数变化**：totalOutCount +1

### 4. 夹具回库

1. 在夹具列表中找到已出库的夹具
2. 点击「回库」按钮
3. 填写：
   - **经办人**（必填）
   - **回库时间**（默认当前时间）
   - **备注**（可选）
4. 点击「确认回库」

**状态变化**：已出库 → 在库  
**计数变化**：totalReturnCount +1

### 5. 夹具报废

1. 点击夹具的「报废」按钮
2. 确认后夹具状态变为「已报废」
3. 报废后不可再出库

## 状态说明

| 状态 | 含义 | 可执行操作 |
|------|------|-----------|
| 🟢 在库 | 夹具存放在仓库 | 出库、报废 |
| 🟡 已出库 | 夹具被领用人取走 | 回库、报废 |
| 🔴 维修中 | 夹具送修保养 | 报废 |
| ⚫ 已报废 | 夹具已剔除 | 无 |

## 流水记录

每个出库、回库、报废操作都会生成流水记录，包含：
- 操作类型（出库/回库/报废）
- 夹具编号和名称
- 状态流转（fromStatus → toStatus）
- 经办人和领用人
- 操作时间和备注

## 数据导出

点击「📊 导出CSV」可导出夹具清单，包含编号、名称、规格、状态、使用次数等字段。

## 二维码格式

```
SPMS1|2001
 ↑    ↑
系统标识  夹具编号
```

扫码后系统自动解析编号，查找对应夹具。

## 注意事项

1. **编号规则**：4位纯数字，如 2001、2002
2. **状态校验**：只有「在库」状态的夹具才能出库
3. **报废不可逆**：夹具报废后无法恢复
4. **数据备份**：定期导出 CSV 备份夹具数据

---

**v2.33** — 新增夹具管理模块，支持出库/回库重复流转
