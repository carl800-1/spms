# 夹具管理系统设计方案 — v2.33

## 一、业务特点分析

### 1.1 夹具 vs 备品的核心差异

| 维度 | 备件管理（现有） | 夹具管理（新增） |
|------|----------------|----------------|
| 消耗性 | 出库即消耗，数量减少 | **可重复使用，数量不变** |
| 追踪维度 | 批次/数量 | **个体/状态** |
| 生命周期 | 入库→出库→用完 | **入库→出库→回库→再出库→...→报废** |
| 价值维度 | 单价×数量 | 单件追踪、使用次数、维修记录 |
| 库存概念 | 总库存数量 | **每个夹具独立状态** |

### 1.2 核心业务场景

```
场景 1：正常流转
  新建夹具（在库）→ 领用人扫码出库（已出库）→ 归还扫码（已回库/在库）→ 再次领用...

场景 2：维修流程
  出库使用中 → 故障送修（维修中）→ 维修完成（在库）→ 再次出库

场景 3：报废流程
  任何状态 → 报废确认（已报废，不可再出库）
```

## 二、状态机设计

```
                        ┌──────────────────────────────────────┐
                        │        ┌──────────────────┐           │
                        │        │   📦 在库        │           │
                        │        │  STATUS: stocked │           │
                        │        └────────┬─────────┘           │
                        │                 │                     │
                        │         ┌───────▼────────┐            │
                        │         │   🔧 维修中    │            │
                        │         │  STATUS: repair │            │
                        │         └───────┬────────┘            │
                        │                 │                     │
                        │         ┌───────▼────────┐            │
                        └────────►│   📤 已出库    │◄────────────────────────────────┐
                                  │ STATUS: checked_out│                              │
                                  └───────┬────────┘                              │
                                          │                                          │
                              ┌───────────┴───────────┐                             │
                              │                       │                             │
                      ┌───────▼───────┐        ┌───────▼───────┐                     │
                      │   ✅ 已回库   │        │   📦 在库    │                     │
                      │ STATUS:       │        │ STATUS:      │                     │
                      │   returned    │        │   stocked    │                     │
                      └───────────────┘        └───────────────┘                     │
                              │                       │                             │
                              │               ┌───────┴────────┐                     │
                              │               │   🗑️ 报废      │                     │
                              │               │ STATUS: retired │                     │
                              │               └────────────────┘                     │
                              │                                                      │
                              └──────────────────────────────────────────────────────┘
                                          扫码回库确认
```

**状态流转规则：**

| 操作 | 前置状态 | 后置状态 | 说明 |
|------|---------|---------|------|
| 出库 | stocked / returned | checked_out | 领用人扫码确认 |
| 回库 | checked_out | stocked | 归还人扫码确认 |
| 送修 | 任意 | repair | 标记维修状态 |
| 报废 | 任意 | retired | 不可逆，禁止再次出库 |

## 三、数据模型设计

### 3.1 夹具主表 `fixtures`

```javascript
{
  id: number,                    // 自增主键（系统内部）
  code: string,                  // 4位编号（如 "2001"），唯一标识
  name: string,                  // 夹具名称（如 "冲压模-A01"）
  spec: string,                  // 规格型号
  category: string,              // 分类（如 "冲压"、"焊接"、"装配"）
  location: string,              // 库位（可选，与备件共用库位体系）
  qrToken: string,               // 二维码内容 "SPMS1|2001"
  status: string,                // 'stocked' | 'checked_out' | 'repair' | 'retired'
  
  // 统计字段（累计计数）
  totalOutCount: number,         // 累计出库次数
  totalReturnCount: number,      // 累计回库次数
  repairCount: number,           // 累计维修次数
  
  // 时间戳
  createdAt: number,             // 建档时间
  updatedAt: number              // 最后更新時間
}
```

### 3.2 夹具流水表 `fixTransactions`

```javascript
{
  id: number,                    // 自增主键
  fixtureId: number,             // 关联夹具ID（核心关联）
  fixtureCode: string,           // 夹具编号（冗余，便于查询）
  fixtureName: string,           // 夹具名称（冗余）
  
  type: string,                  // 'out' | 'return' | 'in' | 'repair' | 'retire'
  operator: string,              // 经办人
  counterparty: string,          // 领用人/归还人/使用部门
  time: number,                  // 操作时间
  remark: string,                // 备注
  
  // 状态流转信息
  fromStatus: string,            // 操作前状态
  toStatus: string,              // 操作后状态
  
  createdAt: number              // 记录创建时间
}
```

### 3.3 数据关系图

```
┌─────────────────┐         ┌─────────────────────────────┐
│   fixtures      │         │   fixTransactions           │
├─────────────────┤         ├─────────────────────────────┤
│ id (PK)         │──1:N───►│ fixtureId (FK)              │
│ code            │         │ fixtureCode (冗余)          │
│ name            │         │ type                        │
│ spec            │         │ operator                    │
│ category        │         │ counterparty                │
│ location        │         │ time                        │
│ status          │         │ fromStatus → toStatus       │
│ qrToken         │         │ remark                      │
│ totalOutCount   │         │ createdAt                   │
│ totalReturnCount│         └─────────────────────────────┘
│ repairCount     │
│ createdAt       │
│ updatedAt       │
└─────────────────┘
```

## 四、二维码关联设计

### 4.1 编码规范

```
二维码内容格式：SPMS1|<夹具编号>
示例：SPMS1|2001

前缀部分：
  SPMS1  — 系统标识（与备件系统共用前缀，便于统一扫码）
  |      — 分隔符
  2001   — 夹具编号（4位纯数字，与备件编号体系独立）
```

### 4.2 二维码与业务关联

```
┌─────────────────────────────────────────────────────────────┐
│                      二维码使用流程                           │
└─────────────────────────────────────────────────────────────┘

出库流程：
  1. 用户点击「出库」按钮
  2. 系统打开扫码界面（摄像头/图片上传）
  3. 扫描夹具二维码
  4. 系统解析：SPMS1|2001 → 编号 2001
  5. 查找夹具：data.fixtures.find(f => f.code === '2001')
  6. 校验状态：status === 'stocked' || 'returned'
  7. 填写领用人信息，提交出库
  8. 创建流水记录，更新夹具状态为 'checked_out'

回库流程：
  1. 用户点击「回库」按钮
  2. 系统打开扫码界面
  3. 扫描夹具二维码
  4. 系统解析编号，查找夹具
  5. 校验状态：status === 'checked_out'
  6. 填写经办人信息，提交回库
  7. 创建流水记录，更新夹具状态为 'stocked'
```

### 4.3 复用现有二维码功能

- **生成**：建档时自动生成 `qrToken = 'SPMS1|' + code`
- **打印**：复用 `renderLabels()` 函数，支持批量打印夹具标签
- **扫码**：复用 `jsQR.js` 解码库，扫码接口 `window.openScan()`
- **解析**：复用 `parseQRToken()` 函数剥离前缀

## 五、出库/回库业务流程

### 5.1 完整业务流程图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        夹具出库流程                                   │
└─────────────────────────────────────────────────────────────────────┘

用户点击「出库」
      │
      ▼
┌───────────────┐
│ 打开出库表单   │
│ • 经办人      │
│ • 领用人      │
│ • 出库时间    │
│ • 备注        │
└───────┬───────┘
        │
        ▼
┌───────────────┐
│ 扫码确认夹具   │◄────── 可选：直接输入编号
│ 解析二维码    │
│ 校验状态      │
└───────┬───────┘
        │
        ▼
┌───────────────┐
│ 提交出库      │
│ • 更新状态    │ stocked → checked_out
│ • 计数+1      │ totalOutCount++
│ • 创建流水    │ type='out'
│ • 保存数据    │
└───────┬───────┘
        │
        ▼
   出库成功 ✓
```

```
┌─────────────────────────────────────────────────────────────────────┐
│                        夹具回库流程                                   │
└─────────────────────────────────────────────────────────────────────┘

用户点击「回库」
      │
      ▼
┌───────────────┐
│ 打开回库表单   │
│ • 经办人      │
│ • 回库时间    │
│ • 备注        │
└───────┬───────┘
        │
        ▼
┌───────────────┐
│ 扫码确认夹具   │◄────── 可选：直接输入编号
│ 解析二维码    │
│ 校验状态      │
└───────┬───────┘
        │
        ▼
┌───────────────┐
│ 提交回库      │
│ • 更新状态    │ checked_out → stocked
│ • 计数+1      │ totalReturnCount++
│ • 创建流水    │ type='return'
│ • 保存数据    │
└───────┬───────┘
        │
        ▼
   回库成功 ✓
```

### 5.2 状态校验逻辑

```javascript
// 出库校验
if (fixture.status !== 'stocked' && fixture.status !== 'returned') {
  throw new Error('夹具当前状态不允许出库');
}

// 回库校验
if (fixture.status !== 'checked_out') {
  throw new Error('夹具未处于已出库状态');
}
```

## 六、需要修改和新增的文件

### 6.1 修改文件清单

| 文件 | 修改内容 | 行数变化 |
|------|---------|---------|
| `package.json` | 版本号 2.32→2.33，description 更新 | ~5 |
| `main.js` | 新增夹具 IPC 通道注册 | +6 |
| `preload.js` | 暴露夹具 API 给渲染进程 | +6 |
| `store.js` | 新增夹具数据操作函数 | +80 |
| `js/db.js` | 新增 IndexedDB/LS 夹具操作 | +60 |
| `js/app.js` | 新增夹具管理视图和逻辑 | +200 |
| `css/style.css` | 新增夹具卡片样式 | +40 |
| `index.html` | 新增导航按钮 | +2 |

### 6.2 新增文件清单

| 文件 | 说明 |
|------|------|
| `fixtures/app.js` | 夹具模块独立实现（可选，主逻辑在 app.js） |

## 七、API 设计

### 7.1 主进程 IPC 通道

| 通道 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `fixture-add` | 新建夹具 | fixture 对象 | id |
| `fixture-update` | 更新夹具 | fixture 对象 | id |
| `fixture-del` | 删除夹具 | id | - |
| `fixture-getAll` | 获取所有夹具 | - | 数组 |
| `fixture-addTransaction` | 创建流水 | tx 对象 | id |
| `fixture-getTransactions` | 获取流水 | - | 数组 |

### 7.2 前端 DB 接口

```javascript
// 夹具 CRUD
DB.fixtureAdd(fixture)              // Promise<number>
DB.fixtureUpdate(fixture)           // Promise<number>
DB.fixtureDel(id)                   // Promise<void>
DB.fixtureGetAll()                  // Promise<Fixture[]>

// 夹具流水
DB.fixtureAddTransaction(tx)        // Promise<number>
DB.fixtureGetTransactions()         // Promise<Transaction[]>
```

## 八、分阶段实施步骤

### 阶段一：数据层改造（已完成 ✓）

- [x] `store.js`：新增 `fixtures`、`fixTransactions` 表初始化
- [x] `store.js`：实现 `addFixture()`、`updateFixture()`、`delFixture()`
- [x] `store.js`：实现 `addFixTransaction()` 状态流转逻辑
- [x] `js/db.js`：Electron 模式 IPC 转发
- [x] `js/db.js`：浏览器模式 localStorage 后备

### 阶段二：主进程改造（已完成 ✓）

- [x] `main.js`：注册 6 个夹具 IPC 通道
- [x] `preload.js`：暴露 6 个夹具 API 给渲染进程

### 阶段三：前端页面改造（已完成 ✓）

- [x] `index.html`：新增「🔩 夹具管理」导航按钮
- [x] `js/app.js`：新增状态常量、视图渲染、业务流程函数
- [x] `js/app.js`：集成到 `RENDERERS` 映射
- [x] `css/style.css`：新增夹具卡片网格样式

### 阶段四：版本升级（已完成 ✓）

- [x] `package.json`：版本号 2.33.0
- [x] `index.html`：标题更新
- [x] `main.js`：窗口标题更新
- [x] `js/db.js`：APP_VERSION 更新

## 九、关键代码片段

### 9.1 夹具出库提交逻辑

```javascript
window.submitFixtureOut = async function (id) {
  const f = state.fixtures.find((x) => x.id === id);
  if (!f) { toast('夹具不存在', 'err'); return; }
  
  const operator = $('#fix_out_op').value.trim();
  const counterparty = $('#fix_out_party').value.trim();
  const time = inputToMs($('#fix_out_time').value);
  const remark = $('#fix_out_remark').value.trim();
  
  if (!operator) { toast('请填写经办人', 'err'); return; }
  
  try {
    await DB.fixtureAddTransaction({
      fixtureId: id,
      type: 'out',
      operator,
      counterparty,
      time,
      remark
    });
    closeModal();
    await reloadAll();
    renderFixtures();
    toast('出库成功', 'ok');
  } catch (e) {
    toast('出库失败：' + e.message, 'err');
  }
};
```

### 9.2 夹具回库提交逻辑

```javascript
window.submitFixtureReturn = async function (id) {
  const f = state.fixtures.find((x) => x.id === id);
  if (!f) { toast('夹具不存在', 'err'); return; }
  
  const operator = $('#fix_ret_op').value.trim();
  const time = inputToMs($('#fix_ret_time').value);
  const remark = $('#fix_ret_remark').value.trim();
  
  if (!operator) { toast('请填写经办人', 'err'); return; }
  
  try {
    await DB.fixtureAddTransaction({
      fixtureId: id,
      type: 'return',
      operator,
      counterparty: '',
      time,
      remark
    });
    closeModal();
    await reloadAll();
    renderFixtures();
    toast('回库成功', 'ok');
  } catch (e) {
    toast('回库失败：' + e.message, 'err');
  }
};
```

### 9.3 数据层状态流转逻辑

```javascript
function addFixTransaction(tx) {
  load();
  const i = data.fixtures.findIndex((f) => f.id === tx.fixtureId);
  if (i < 0) throw new Error('夹具不存在');
  const fixture = data.fixtures[i];
  
  // 状态校验
  if (tx.type === 'out' && fixture.status !== 'stocked' && fixture.status !== 'returned') {
    throw new Error('夹具当前状态不允许出库');
  }
  if (tx.type === 'return' && fixture.status !== 'checked_out') {
    throw new Error('夹具未处于已出库状态');
  }
  
  const fromStatus = fixture.status;
  let toStatus = fixture.status;
  
  // 更新状态和计数
  if (tx.type === 'out') {
    toStatus = 'checked_out';
    fixture.totalOutCount = (fixture.totalOutCount || 0) + 1;
  } else if (tx.type === 'return') {
    toStatus = 'stocked';
    fixture.totalReturnCount = (fixture.totalReturnCount || 0) + 1;
  } else if (tx.type === 'repair') {
    toStatus = 'repair';
    fixture.repairCount = (fixture.repairCount || 0) + 1;
  } else if (tx.type === 'retire') {
    toStatus = 'retired';
  }
  
  fixture.status = toStatus;
  fixture.updatedAt = Date.now();
  
  // 创建流水记录
  const record = {
    id: nextId(),
    fixtureId: fixture.id,
    fixtureCode: fixture.code,
    fixtureName: fixture.name,
    type: tx.type,
    operator: tx.operator || '',
    counterparty: tx.counterparty || '',
    time: tx.time,
    remark: tx.remark || '',
    fromStatus, toStatus,
    createdAt: Date.now()
  };
  
  data.fixTransactions.push(record);
  save();
  return record.id;
}
```

## 十、测试要点

### 10.1 功能测试

- [ ] 新建夹具：编号 4 位数字，名称必填
- [ ] 夹具出库：状态校验、流水记录、计数更新
- [ ] 夹具回库：状态校验、流水记录、计数更新
- [ ] 夹具报废：状态变更、不可逆
- [ ] 二维码生成：正确编码 SPMS1|2001
- [ ] 扫码出库：正确解析、正确查找
- [ ] 扫码回库：正确解析、正确查找
- [ ] 流水记录：完整记录所有操作

### 10.2 边界测试

- [ ] 出库时状态非在库：应拒绝并提示
- [ ] 回库时状态非已出库：应拒绝并提示
- [ ] 重复出库：应拒绝
- [ ] 重复回库：应拒绝
- [ ] 报废后出库：应拒绝
- [ ] 编号重复：应拒绝

### 10.3 数据测试

- [ ] 新建夹具后 data.fixtures 新增记录
- [ ] 出库后流水表新增记录，状态更新
- [ ] 回库后流水表新增记录，状态更新
- [ ] 计数字段正确累加
- [ ] 时间戳正确记录

## 十一、后续扩展建议

### 11.1 短期优化

1. **扫码集成**：将二维码扫描直接集成到出库/回库表单，无需先点按钮
2. **批量出库**：支持一次性扫多个夹具二维码批量出库
3. **出库历史记录**：按夹具查看完整流转历史

### 11.2 中期增强

1. **维修记录**：维修表单记录故障原因、维修人、维修成本
2. **夹具寿命**：累计出库次数达到阈值自动提醒寿命到期
3. **夹具位置**：出库时记录使用位置，回库时对比

### 11.3 长期规划

1. **夹具借用周期统计**：平均借用时长、高峰时段
2. **夹具利用率分析**：闲置夹具识别
3. **夹具折旧计算**：按使用次数估算价值

---

**版本**：v2.33  
**日期**：2026-09-02  
**作者**：WorkBuddy AI Assistant
