# 备件仓库管理系统 — 修改日志

## v2.32（2026-08-13）

### 变更（库位总览改为 10 列网格 + 三行可视）

1. **库位总览改为 10 列网格 + 三行可视**：库位卡片网格由 12 列等宽改为 **10 列等宽**（`grid-template-columns: repeat(10,1fr)`），其余保持三行可视、内容区域可上下滑动垂直浏览全部库位（共 115 个）；卡片随容器宽度自适应缩放、按行列规则整齐排列，适配不同屏幕尺寸。
2. **卡片仍仅显示库位编号**：保留 v2.30 起的简洁风格，卡片只显示库位编号（如 A1-03-02），悬停仍可查看件数与预警信息；选中库位时纵向滚动位置保持不变。
3. **版本号升级至 2.32**。

---

## v2.31（2026-08-13）

### 变更（库位总览改为 12 列网格 + 三行可视）

1. **库位总览改为 12 列网格 + 三行可视**：库位卡片网格由三列等宽改为 **12 列等宽**（`grid-template-columns: repeat(12,1fr)`），并固定为 **三行可视**（`max-height` 限制为 3 行高度），内容区域可上下滑动垂直浏览全部库位（共 115 个）；卡片随容器宽度自适应缩放、按行列规则整齐排列，适配不同屏幕尺寸。
2. **卡片仍仅显示库位编号**：保留 v2.30 的简洁风格，卡片只显示库位编号（如 A1-03-02），悬停仍可查看件数与预警信息；选中库位时纵向滚动位置保持不变。
3. **版本号升级至 2.31**。

---

## v2.30（2026-08-13）

### 变更（库位总览改为三列网格 + 垂直滚动，仅显示编号）

1. **库位总览改为三列网格 + 垂直滚动**：库位卡片由横向滑动条改为三列等宽网格（`grid-template-columns: repeat(3,1fr)`），内容区域可上下滑动垂直浏览全部库位，布局紧凑、滑动流畅，并适配不同屏幕尺寸（窄屏仍保持三列）。
2. **卡片仅显示库位编号**：移除了卡片下方的备品件数 / 库存预警小数字，界面更简洁；悬停卡片仍可查看该库位的件数与预警信息（title 提示）。选中库位时纵向滚动位置保持不变。
3. **版本号升级至 2.30**。

---

## v2.29（2026-08-13）

### 变更（库位总览改为上下分屏 + 横向滑动小卡片）

1. **库位总览改为上下分屏布局**：上方为库位信息区，下方展示所选库位对应的具体物品列表；整体支持滑动浏览，布局紧凑、操作流畅。
2. **卡片缩小为约原 1/4 并改为横向滑动**：库位卡片尺寸约 60×46px（约原 1/4 面积），上方库位区改为单行横向滑动浏览（scroll-snap，每行可容纳更多库位）；点选库位后上方横向位置保持不变。连续编号 001–NNN 与库存预警徽章均保留。
3. **版本号升级至 2.29**。

---

## v2.28（2026-08-13）

### 变更（库位总览改为连续编号卡片网格）

1. **库位总览改为统一方框（卡片）网格**：原表格改为统一尺寸卡片，每行固定 8 个、等宽等高、排满自动换行；卡片显示库位连续编号 / 备品件数 / 库存预警，点击卡片筛选、当前库位高亮，整体排列有序、便于快速浏览。
2. **采用新的连续编号方案重排库位**：原混杂编码（如 1-1-1、1-2-1上、A1-02-03）统一迁移为 001 起递增的连续编号（001–NNN），所有库位按新编号排序展示；启动时会自动迁移历史数据并置位标记（幂等），旧货架标签请按「库位重编号映射表」重打。
3. **版本号升级至 2.28**。

---

## v2.27（2026-08-13）

### 变更（库位总览布局与排序优化）

1. **库位总览改为统一表格**：原换行过滤按钮（chip）改为规范表格，列含「库位 / 备品件数 / 库存预警 / 操作」，各列对齐、整行可点击筛选，当前选中库位高亮，视觉整齐有序。
2. **按库位编号自然升序**：采用自然排序（数字按数值而非字符序，如 A-2 排在 A-10 前），「（未分配）」始终置于末尾，便于快速浏览与查找。
3. **版本号升级至 2.27**。

---

## v2.26（2026-08-13）

### 变更（「清除已选内容」按钮改蓝色主色样式）

1. **按钮视觉优化**：入库/出库「清除已选内容」按钮由白色 Ghost 样式改为页面统一主色蓝（`.btn sm`，背景 `#2563eb`、文字白色），圆角/字号/内边距/悬停效果与其它主按钮完全一致，消除风格突兀感。
2. **版本号升级至 2.26**。

---

## v2.25（2026-08-12）

### 变更（新增入库/出库「清除已选内容」按钮）

1. **新增「清除已选内容」按钮**：在入库管理与出库管理的「检索结果」面板标题右侧各新增一个「清除已选内容」按钮（Ghost 样式）。点击后一键清空已选备品的编号/名称/规格/库位四栏、重置检索结果列表与提示文字，并将焦点回到编号输入框，便于用户基于新的检索条件重新查询。
2. **根因说明**：选中备品后四栏均被自动回填；若仅修改其中一栏再次检索，其余三栏的残留值会作为 AND 过滤条件锁死结果（旧编号/库位仍匹配原备品），导致系统无法按新条件刷新显示对应备品列表。清除按钮正是为解决此问题而设。
3. **版本号升级至 2.25**。

---

## v2.24（2026-08-12）

### 变更（修复入库/出库「选择」按钮报错 + 编号自动填充）

1. **修复入库/出库「选择」按钮报错**：结果列表行的「选择」按钮原生成的点击调用为未定义的 `in_pick('...')` / `out_pick('...')`，点击即触发 `ReferenceError` 且 `inOutPick` 不执行、字段不回填；改为调用已挂载的 `inbound_pick` / `outbound_pick`，点击「选择」正常回填编号/名称/规格/库位。
2. **修复手动填写编号不自动填充**：原 `fillPartFields` 对聚焦字段刻意跳过回填，用户在编号框打字时该框为 active，编号不会被规整回填；改为命中唯一备品时始终回填规范编号（`code` 字段不再跳过），其余字段保持「不覆盖正在编辑的输入框」逻辑。
3. **版本号升级至 2.24**。

---

## v2.23（2026-08-12）

### 变更（修复查询详情渲染异常 + 移除手动库位盘点）

1. **修复备件查询结果点击展开异常**：备件查询的明细原嵌套在结果表 `<tbody>` 内（`<tr>` 内再嵌完整 `<table>`），在 `display` 切换时 Chromium 内层表格布局常不刷新，导致记录表错位 / 塌陷（如「暂无记录」与表头并列）。改为「结果表下方的独立容器」渲染，点击任意结果行稳定展开 / 收起明细。
2. **移除「手动库位盘点」打印入口**：库位管理不再提供「📋 手动库位盘点」打印清单功能；保留「📋 手工盘点（导出 CSV）」。
3. **「盘点人」录入方式调整**：原由「手动库位盘点」界面填写的盘点人，改为点击「手工盘点」导出时录入（弹出输入框，预填上次记忆值，取消则中止导出），导出 CSV 的「盘点人」列含义不变。
4. **版本号升级至 2.23**。

---

## v2.22（2026-08-12）

### 变更（库位管理「手工盘点」导出改为逐项明细）

1. **「手工盘点」导出内容升级**：导出由「按库位聚合（库位号 / 件数 / 盘点人）」改为「按备品逐项明细」，列包含：编号、名称、规格、分类、单位、库位、安全库存、当前库存、状态、盘点人。
2. **盘点人字段来源**：「盘点人」取自当前「手动库位盘点」界面所填盘点人；点击「手工盘点」导出前可在该界面填入盘点人，便于纸质逐项核对与责任登记。
3. **版本号升级至 2.22**。

---

## v2.21（2026-08-12）

### 变更（备件查询移除扫码盘点 + 库位管理新增「手工盘点」导出）

1. **移除「扫码盘点」**：在「备件查询」模块中删除顶部的「📷 扫码盘点」按钮及对应 `scanStocktake` 函数；备件明细内的「🧮 盘点此备件」手动盘点入口保留不变。
2. **库位管理新增「手工盘点」按钮**：在「库位管理」模块按钮区新增「📋 手工盘点」按钮，其实现逻辑与「备件查询」的「导出 CSV」完全一致（相同 `toCSV + download` 流程），点击后将库位聚合数据（库位号 / 件数 / 盘点人）导出为 CSV 文件。
3. **版本号升级至 2.21**。

---

## v2.20（2026-08-12）

### 变更（库位盘点更名为「手动库位盘点」+ 打印清单）

1. **功能更名**：「库位管理」模块中原「📋 库位盘点」入口更名为「📋 手动库位盘点」。
2. **点击直达打印清单**：点击后直接进入库位盘点打印清单页（不再有「填写盘点人→开始盘点」的中间表单步骤），界面更聚焦。
3. **清单字段**：按库位列出四列——「已完成」勾选框、「库位号」、「件数」（该库位下备品数量）、「盘点人」；勾选框用于标记该库位已盘点，底部实时统计「已标记 N 个」。
4. **打印输出**：点击「🖨 打印盘点清单」生成独立打印页（A4、含盘点人/盘点日期页眉、签字栏与制表人/复核人签名位），布局清晰，适合纸质打印后现场逐项核对打钩。
5. **版本号升级至 2.20**。

---

## v2.19（2026-08-11）

### 变更（库位盘点功能）

1. **库位盘点功能新增**：在「库位管理」模块新增「📋 库位盘点」入口，支持完整的库位盘点流程。
2. **盘点表单交互**：盘点前填写「盘点人姓名」与「盘点日期」，确认后进入盘点表界面。
3. **逐项核对确认**：盘点表按库位-备品展示完整信息（库位/编号/名称/规格/账面库存），每行提供「✓ 勾选框」用于确认核对，并支持输入「盘点数量」自动计算「差异」（账面库存 vs 实际盘点数量，正差异绿色、负差异红色）。
4. **盘点记录提交**：全部勾选确认后点击「提交盘点记录」，数据落库至 `locationAudits` 表，历史盘点记录可在库位盘点页面查看。
5. **打印盘点表**：点击「🖨 打印盘点表」生成标准打印页，包含盘点日期、盘点人签名空位、复核人签名空位，打印格式清晰规范。
6. **版本号升级至 2.19**。

---

## v2.18（2026-08-11）

### 变更（入库/出库检索互查 + 备品建档闪退修复）

1. **入库/出库「四栏互查」检索**：登记入库、登记出库均新增「名称」「规格」两个检索栏，与原有的「备品编号」「库位号」形成**四栏互查**——任一栏输入或扫码即检索；命中唯一备品时**自动回填其余三栏**，多命中时在下方「检索结果」列表点选即回填。覆盖正向（编号→名称/规格/库位）、反向（库位→编号）及新增的名称/规格→其余字段联动。
2. **「检索结果」列表替代「最近入库/出库记录」**：原固定展示最近流水记录的面板，改为直接以检索结果列表作为展示内容（编号/名称/规格/库位/当前库存/状态 + 选择），检索为空时给出操作提示；提交时按任意已填检索栏解析目标备品，多匹配需点选确认。
3. **修复备品建档闪退（以零开头编号兼容）**：`num()` 数值解析全面加固——兼容以零开头（`01`）、全角数字（`０１`）、前后空格、千分位逗号等格式，始终返回安全数字，杜绝因输入格式导致 `NaN` / 崩溃；安全库存、库存、出入库数量等字段均走该健壮解析，并在载入时强制数值化。**再次强调：编号由系统自动分配 4 位纯数字，仍是只读预填，不应手工输入。**
4. **出库管理同步改造**：交互与数据逻辑（四栏互查 + 检索结果列表 + 健壮解析）与入库管理保持一致。
5. **版本号升级至 2.18**，上一稳定版 v2.17 归档于 `release/backup/2.17/`（可作回滚）。

---

## v2.17（2026-08-10）

### 变更（缺陷修复：记录查询兼容扫描枪二维码前缀）

1. **记录查询剥离二维码前缀**：`js/app.js` 的 `queryRecords()` 对「备品名称/编号」搜索关键字先调用 `parseQRToken()` 剥离 `SPMS1|` / `SPMS|` 前缀，再与流水记录的 `partName + partCode` 做包含匹配。解决扫描枪把完整二维码内容（如 `spms1|1045`）直接键盘模拟进输入框时，查询条件因带前缀而无法命中 `partCode='1045'` 记录的问题。
2. **记录查询增加扫码按钮**：在「备品名称/编号」输入框旁新增 📷 扫码按钮，调用 `scanForCode('r_name', renderRecordsResults)`，与入库/出库/备件查询的扫码体验保持一致。
3. **版本号升级至 2.17**，上一稳定版 v2.16 归档于 `release/backup/2.16/`（可作回滚）。

---

## v2.16（2026-07-31）

### 变更（打印选项 + 预览 + 不同纸张居中）

1. **新增打印设置面板**：批量打印标签 / 单个标签打印新增设置项——
   - **纸张尺寸**：A4、A5、小票纸 58mm、小票纸 80mm、标签纸 100×150mm，以及**自定义宽高**（mm）。
   - **内容对齐**：水平（左 / 居中 / 右）、垂直（顶部 / 居中 / 底部）。
   - **页边距**：上 / 右 / 下 / 左（mm）独立可调。
   - **份数**：1 起可调。
   - 上述设置经 `localStorage` 记忆，下次打开自动恢复。
2. **打印预览**：弹窗内按所选纸张比例与对齐方式**实时模拟**打印效果（WYSIWYG），所见即所得，避免小纸张上内容偏位。
3. **底层改造（解决小纸张无法居中）**：
   - `main.js` 新增 `print-labels` IPC，调用 `webContents.print()`，按纸张尺寸（微米 `{width,height}` 或标准名如 `A4`）与份数精确送印，`printBackground:true` 保留标签边框。
   - `preload.js` 暴露 `window.api.printLabels()`。
   - `css/style.css` 的 `@media print` 由「visibility 隐藏」改为「仅保留 `#printArea`」，并以 **flex 布局 + CSS 变量（`--ph` 水平 / `--pv` 垂直 / `--pm` 边距）** 实现按设置居中；`#printArea` 设 `min-height:100vh`，少量标签可在整页内垂直居中，多标签自动换行流式分页。
   - `js/app.js` 重构打印逻辑：抽出 `labelCardHTML()/renderLabels()` 复用标签渲染；`resolvePageSize()` 将预设/自定义尺寸换算为电子打印所需单位；`renderPreview()` 按 96dpi 将纸张等比缩放绘制到预览舞台。
4. **版本号升级至 2.16**，上一稳定版 v2.15 归档于 `release/backup/2.15/`（可作回滚）。

---

## v2.15（2026-07-30）

### 变更（编号位数调整：6 位 → 4 位纯数字，起始 1001）

1. **编号格式调整为 4 位纯数字**：在 v2.14「编号统一管理方案」基础上，将所有备品编号由 `100001` 起的 6 位纯数字调整为 `1001` 起的 **4 位纯数字**（如 `1001`、`1002`、`1003`）。其余设定（自动顺序分配、只读预填、一次性迁移、扫码/二维码兼容、CSV 导入适配）保持不变。
2. **定义与校验同步更新**（保持全模块一致）：
   - `store.js`：`isValidCode()` 正则由 `/^\d{6}$/` 改为 `/^\d{4}$/`；一次性迁移 `migrateCodes()` 起始号由 `100001` 改为 `1001`。
   - `js/app.js`：`nextPartCode()` 起始基准 `100000`→`1000`、匹配正则 `\d{6}`→`\d{4}`；`submitPart()` 校验 `/^\d{6}$/`→`/^\d{4}$/`；导入编号强制校验、模板示例、入库/出库/查询占位符、建档占位符均统一为 4 位 `1001` 起。
3. **一次性迁移（幂等）**：已按 v2.14 迁移好的 6 位号，在 v2.15 首次打开时自动重排为 4 位号（`1001…`），并同步更新流水 `partCode` 显示字段（partId 关联不变，历史不丢）。全部已是 4 位时直接跳过，重跑无害。
4. **版本号升级至 2.15**，上一稳定版 v2.14 归档于 `release/backup/2.14/`（可作回滚）。

---

## v2.14（2026-07-29）

### 变更（功能升级：备件编号统一管理方案 —— 6 位纯数字 + 自动顺序分配 + 一次性迁移）

1. **编号格式统一为 6 位纯数字**：所有备品编号统一为 `100001` 起、6 位纯数字格式。彻底消除扫描枪因编号含字母 / 连字符 / 长度不一导致的识别与匹配问题，标签也更简洁可读。
2. **新建自动顺序分配**：建档（新增）时系统按「当前最大 6 位号 +1」（最小 `100001`）自动分配编号，保证唯一、无重复、无随机跳号；建档编号框改为**只读预填**，仅保留「↻ 重新分配」取下一个号。
3. **一次性数据迁移**（store.js `load()` 内 `migrateCodes()`，幂等）：
   - 首次打开存量数据时，将非 6 位纯数字编号（如 `BJ-001`、`SP20240101-001`、超长数字等）按**建档顺序（id）**稳定重排为 `100001…`；已合法的 6 位数字保留不动；跳过已占用号防止冲突。
   - **同步更新 `transactions.partCode` 显示字段**（partId 关联不变，历史流水不丢）。
   - 迁移后自动落盘；全部已是 6 位号时直接跳过，重跑无害。
4. **CSV 导入适配**：导入模板示例改为 `100001` / `100002`；导入数据中编号若非合法 / 唯一 6 位数字，自动分配下一个 6 位号（`occupied` 集合防重）。
5. **扫码 / 二维码兼容**：二维码内容仍为 `SPMS1|<编号>`，扫描枪读取后剥离前缀即得 6 位数字，匹配更稳定；标签显示同步为 6 位。⚠ 旧版已打印标签编码的是旧编号，升级后需用「批量打印」重新打印。
6. **版本号升级至 2.14**，上一稳定版 v2.13 归档于 `release/backup/2.13/`（可作回滚）。

---

## v2.13（2026-07-29）

### 变更（Bug 修复：备品编号连字符 / 大小写差异导致查不到）

1. **【Bug 修复】编号格式差异导致匹配失败**：扫描枪或手动录入的编号若与库中编号存在分隔符差异（如扫描得到 `BJ003` 而库中为 `BJ-003`，或缺空格 / 大小写不同），此前所有匹配入口都用严格相等（`p.code === code`），因此搜不到。
   - 新增 `normalizeCode()` 归一化函数：先 `parseQRToken` 剥离二维码前缀 `SPMS1|` / `SPMS|`，再忽略大小写，并忽略分隔符差异（连字符“-”、下划线“_”、空格），使 `BJ003` 与 `BJ-003` / `bj_003` 可互匹配。
   - 在**全部**编号匹配入口启用归一化：登记入库 / 出库（`resolvePartByCode` / `resolveOutByCode`）、备件查询搜索（`qySearchList` 编号容错匹配 + 名称/规格/库位/分类原值匹配）、扫码快速盘点（`scanStocktake`）、入库/出库提交（`submitInbound` / `submitOutbound`）。匹配成功后回填库中的**规范编号**（保留连字符），保证下游联动与落库一致。
   - 已打印的旧二维码 / 标签无需重打，下次扫描自动归一化命中。
2. **边界处理**：
   - 匹配 **0 个** → 提示「⚠ 未找到编号…对应的备品（已忽略连字符/大小写差异）」；
   - 匹配 **多个** → 自动选首个规范编号并提示「⚠ 匹配到 N 个编号，已选 X；如需其它请手动选择」（备件查询页天然列出所有命中并显示数量）；
   - 扫码盘点匹配多个时弹「匹配到 N 个，已选 X」提示。
3. **顺带修复**：登记入库 `submitInbound` 原漏写 `partId`（写成了未定义的 `partId` 而非 `part.id`），导致入库流水未正确关联备品、在记录查询/明细中无法回溯；本次一并修正为 `partId: part.id`。
4. **版本号升级至 2.13**，上一稳定版 v2.12 归档于 `release/backup/2.12/`。

---

## v2.12（2026-07-29）

### 变更（Bug 修复：全场景扫码定位备品 + 版本命名规范调整）

1. **【Bug 修复】全场景扫码枪 / 二维码定位备品**：将二维码前缀 `SPMS1|<code>`（兼容历史 `SPMS|<code>`）的归一化覆盖到**全部**编号输入入口，确保无论哪种方式录入都能正确匹配：
   - 登记入库 / 登记出库：扫码枪键入（键盘模拟）、📷 摄像头 / 拍照识别、手动输入，统一经 `parseQRToken` 剥离前缀后匹配并回写纯净编号（`resolvePartByCode / resolveOutByCode / submitInbound / submitOutbound`）。
   - **备件查询**：① 扫描枪直接把 `SPMS1|...` 键入顶部搜索框时，`qyRun` 先归一化剥离前缀再做检索（此前会整体搜不到）；② 📷「扫码识别备品」按钮 `scanForQyQ` 归一化后再带入。
   - **扫码快速盘点** `scanStocktake`：归一化后再 `find(x.code === ...)`，否则手动 / 粘贴输入会匹配失败。
   - **建档编号框** `submitPart`：误扫进建档框时自动剥离前缀，避免存成脏编号。
   - 归一化统一在 `scanManualResolve / scanForCode / scanForText / scanForQyQ / scanStocktake` 与 `qyRun / submitPart` 等处入口执行，行为一致。
2. **版本命名规范调整**：**不再使用 2.11.1 这类三位补丁版本号**，统一采用两位版本号（本次 2.12，后续直接递增末位：2.13、2.14 …）。故原计划的「2.11.1 补丁」并入本次 2.12。
3. **版本号升级至 2.12**，v2.11.1（原计划补丁，已并入本版）的相关内容见上方「全场景修复」；上一稳定版 v2.11 归档于 `release/backup/2.11/`。

---

## v2.11（2026-07-29）

### 变更（分发模型改为版本化文件夹）

1. **分发文件夹改名**：对外分发的 `win-unpacked` 目录更名为 **「备件仓库系统v2.11」** 文件夹，版本号以**文件夹名**为准。
   - 程序运行时通过 `process.execPath` 定位数据目录（`exe 同目录`），**重命名父文件夹不影响数据读取与程序运行**。
   - 今后升级：仅需**重命名该文件夹**（如 `备件仓库系统v2.12`）并更新其内部内容（exe / `spms-data.json` 数据），无需改动代码中的路径引用。

2. **归档备份规则更新**：归档时完整备份「备件仓库系统v2.11」文件夹 + 项目源码等其它相关文件，便于整体回滚。

3. **版本号升级至 2.11**，v2.10 归档至 `release/backup/2.10/`。

---

## v2.10（2026-07-29）

### 变更（库存管理与备件盘点合并至「备件查询」统一页）

1. **功能整合**：将「库存管理」与「备件盘点」合并进「备件查询」，以备件查询作为统一入口，消除导航冗余。
   - 顶部筛选条：关键字搜索（编号 / 名称 / 规格 / 库位 / 分类）+ **分类下拉** + **库存状态下拉（全部 / 正常 / 预警 / 缺货）** + 导出 CSV + 📷 扫码盘点。
   - 结果表直接展示**当前库存 / 安全库存 / 状态 / 最近动态**，备件信息与库存状态在同一视图联动。
   - 点任意行展开明细，明细内新增「🧮 盘点此备件」按钮，可直接发起盘点；页底保留「最近盘点记录」。
   - 库存预警：原「仅看预警」复选框升级为「库存状态」下拉（正常 / 预警 / 缺货），覆盖更全面。
   - 盘点提交后实时 `reloadAll` 并重渲染统一页，库存与备件信息保持同步一致。

2. **移除独立导航项**：侧边栏不再显示「库存管理」「备件盘点」，二者能力已并入「备件查询」；库位管理、记录查询保持独立（不同维度）。

3. **版本号升级至 2.10**，v2.9 归档至 `release/backup/2.9/`。

---

## v2.9（2026-07-28）

### 变更（修复搜索框中文输入法冲突）

1. **修复搜索框中文输入法（IME）冲突**
   - 备品管理 / 库存管理 / 记录查询的搜索框改为「**输入框常驻、仅刷新结果区**」模式：搜索输入框在输入过程中**不再被重建**，仅更新独立的结果容器（`#partResults` / `#invResults` / `#recResults`）。
   - 监听 `compositionstart / compositionend`：中文组合输入进行中**跳过刷新、不打断**，组合结束后再刷新结果，彻底解决「输入中文时无法上屏、每按一键重复出现两个英文字母」的问题。
   - 经办人拼音联想、盘点备品搜索的候选下拉增加 `isComposing` 守卫，组合中输入不打断、不闪候选。
   - 中、英文输入均正常工作；系统架构与既有功能不变。

2. **版本号升级至 2.9**，v2.8 归档至 `release/backup/2.8/`。

---

## v2.8（2026-07-28）

### 变更（备件查询：改为「结果列表」式多匹配）

1. **备件查询改为「结果列表」模式**
   - 输入关键字后**实时列出所有匹配的备件记录**（不再进入候选下拉逐条选择），尤其适合「电磁阀」等多匹配场景的批量核对。
   - 支持按**备件号 / 名称 / 规格 / 库位 / 分类**多字段模糊搜索（任一字段包含关键字即命中）。
   - 结果表格展示：编号 / 名称 / 规格 / 库位 / 分类 / 当前库存 / 状态 / 最近动态；点击任意一行**展开明细**（库存 + 库位 + 最近 15 条出入库 / 盘点流水）。
   - 顶部保留 📷 扫码按钮（扫码带入编号即列出该备件）与清除按钮。

2. **版本号升级至 2.8**，v2.7 归档至 `release/backup/2.7/`。

### 实现要点

- `app.js` 重写「备件查询」模块：`qySearchList`（多字段模糊搜索，去除 30 条上限）、`qyRun`（渲染匹配结果列表）、`qyToggle`（展开 / 收起明细）、`qyClear`（清除并重查）、`scanForQyQ`（扫码带入编号）、`qyDetailInner`（单备件明细 + 最近流水）。
- 移除旧的下拉选择逻辑：`qySuggest / qyPickFirst / qySelectPart / qyClearPicked / renderQueryAc / renderQueryDetail` 及 `state.qySelectedId` 等不再使用。
- CSS 新增：`.qy-row-click`（可点击行）/ `.qy-detail-row`（展开明细行）/ `.qy-detail`（明细容器）。

---

## v2.7（2026-07-28）

### 变更（备件盘点输入化 + 新增备件查询 + 盘点改名）

1. **「盘点」改名为「备件盘点」**
   - 侧边栏与页面标题统一更名为「**备件盘点**」，明确作用对象为备件库存校正，与入库 / 出库 / 备件查询并列。
   - 与之相关的"扫码 / 选择备品进行盘点"标题不变。

2. **备件盘点表单重构：备品选择改为「输入框 + 扫码」**
   - 备品选择从 `select` 下拉框（600+ 备品时难以查找）改为 `备品编号` **输入框 + 📷 扫码按钮 + 清除按钮**，与「登记入库」「登记出库」保持完全一致的布局和视觉风格。
   - 支持按**备件号 / 名称 / 规格 / 库位 / 分类** 五个字段做模糊搜索，输入即弹出候选下拉（最多 30 条），按 `Enter` 直接选中第一个。
   - 支持扫描枪扫码识别备品编号。
   - 选中后，下方表单联动展示「系统库存」，与「实盘数量 / 经办人 / 备注或用途」在同一行排布（field-grid 4 列），与入库 / 出库表格化风格一致。
   - 经办人沿用拼音模糊联想（继承自 v2.6）。

3. **新增独立「备件查询」功能（左侧导航）**
   - **为什么独立而非整合**：现有三大功能定位清晰互不重叠——
     - 「备品管理」= 增删改 / 批量打印 / CSV 导入导出等管理入口；
     - 「库存管理」= 全部备品的库存只读视图（含预警筛选）；
     - 「记录查询」= 流水筛选（含类型 / 经办人 / 时间）。
     「备件查询」是**以备件为入口**的「只读一站式」检索：用户**已知或临时选中某个备件**，想一次性看到「它是什么 / 现在有多少 / 放在哪 / 最近怎么动的」，与上述三个功能角度不同（不是按"操作"也不是按"全集"），独立列出符合用户"看到一个备件就给我它全部信息"的操作流程。
   - 实现：
     - 顶部单输入框 + 📷 扫码按钮 + 清除按钮，与备件盘点风格一致；按备件号 / 名称 / 规格 / 库位 / 分类 多字段模糊搜索候选下拉。
     - 选中后展示「备件详情」面板（编号 / 名称 / 规格 / 分类 / 单位 / 库位 / 安全库存 / 当前库存 / 状态 / 累计入库 / 累计出库 / 盘点次数 / 最近动态 / 备注或用途）。
     - 下方展示该备件最近 15 条出入库 / 盘点记录（带类型徽标）。
     - 提示用户：如需完整历史流水，请到「记录查询」按编号筛选。

4. **版本号升级至 2.7**，v2.6 归档至 `release/backup/2.6/`。

### 实现要点

- `app.js` 新增模块：`stSearchList / qySearchList`（多字段模糊搜索）、`stSuggest / qySuggest`（候选下拉）、`stPickFirst / qyPickFirst`（回车取首个）、`stSelectPart / qySelectPart`（选中后联动）、`stClearPicked / qyClearPicked`（清除）、`scanForStQ / scanForQyQ`（扫码识别）、`renderQuery`（备件查询视图）、`renderQueryDetail`（详情 + 最近流水）。
- 状态扩展：`state.stSelectedId`（盘点已选备件 id）、`state.qySelectedId`（查询已选备件 id）、`state.qyQuery`（查询关键词）。
- CSS 新增：`.st-ac` / `.st-ac-item` / `.st-ac-code` / `.st-ac-name` / `.st-ac-meta`（候选下拉样式）、`.qy-info` / `.qy-row` / `.qy-key` / `.qy-val`（备件详情网格），沿用 `.field-hint` `.badge` `.op-ac` 既有样式体系。
- 导航与视图注册：`VIEW_TITLES` / `RENDERERS` 同步加入 `query` 视图，盘点 `stocktake` 标题改为「备件盘点」。
- 顶部品牌 `v2.6 → v2.7`，`package.json` 版本号 / `db.js` `APP_VERSION` / `main.js` 窗口标题同步。

### 影响范围

- 备件盘点：表单交互与候选下拉为主要变更；历史盘点记录无影响。
- 新增「备件查询」入口：仅新增，不改动其他模块数据。
- 整体下拉框（`partOptions` / `scanForSelect`）保留为兼容函数，未被新代码调用，下一版本可清理。
- 历史数据完全兼容，无迁移成本。

---

## v2.6（2026-07-28）

### 变更（经办人拼音模糊下拉 + 出库字段调整）

1. **经办人输入增强（入库 / 出库）**
   - 入库「经办人」、出库「领用人」输入框支持**拼音模糊查找 + 下拉选择**：输入中文或拼音（全拼 / 首字母）后自动合并匹配结果，下拉列表展示并支持滚动浏览，点击即填入。
   - 新输入的姓名自动记忆，下次打开可直接联想；名单来源于历史交易 + 本地持久化（刷新保留）。

2. **出库字段调整**
   - 出库「经办人」更名为**「领用人」**。
   - 移除「领用人 / 用途」字段（出库记录不再单独记录用途说明）。

3. 版本号升级至 **2.6**，并将 v2.5 归档保存。

## v2.5（2026-07-28）

### 变更（出库扫码录入 + 库位扫码 + 数据库随程序移动 + About 全版本滚动）

1. **「登记出库」备品选择由下拉框改为输入框，支持扫描枪直接扫码**
   - 与入库一致：出库界面备品选择改为 `备品编号` **输入框**，可直接用扫描枪扫码或手输编号。
   - 输入/扫码后实时解析备品名称，并自动把该备品的已知库位带入「库位号」输入框。

2. **出库新增「库位号」反向查询**
   - 在出库界面输入库位号（如 `A-01-03`），自动查找该库位对应的备品并**自动填写备品编号**；同库位多种备品时列出候选供点选。

3. **库位号支持扫码枪扫码录入**
   - 入库与出库的「库位号」旁均新增 📷 扫码按钮，除手动输入外可**快速扫码**录入库位（扫码枪键盘输入同样可用）。

4. **数据库文件改存于程序所在目录（整体可移动）**
   - 数据文件 `spms-data.json` 由系统 `AppData` 固定路径改为**程序 exe 同目录**，程序与数据库可整体复制 / 移动，避免路径依赖。
   - 首次启动自动把旧版数据从 `AppData` 迁移到新位置，存量数据不丢失。
   - 注：单文件便携版（`.exe`）运行时系统会解压到临时目录，数据随之驻留该目录；如需"复制整个文件夹即带走数据"，建议使用解压后的程序文件夹（数据库就在 exe 旁）。

5. **「关于 / About」面板改为滚动条布局，收录全部历史版本变更**
   - 面板内容过长时内部出现**滚动条**，不再撑破弹窗。
   - 内置 **v2.0 → v2.5 每个版本号及对应更新内容**，便于追溯。

### 实现要点

- 扫码组件重构：`openScan` 回调改为返回**原始扫码文本**（不再是已定位的备品对象），新增 `scanForText(inputId, onResolved)` 通用扫码填入；`scanForCode(inputId, onResolved)` 支持按字段联动。
- 出库新增：`resolveOutByCode()`（编号→备品解析 + 正向联动库位）、`outReverseLookup()`（库位→编号反查 + 多备品候选）、`pickOutReverseCode(code)`（候选点选回填）；`submitOutbound` 改为按编号解析 `partId`。
- `main.js`：`app.isPackaged ? path.dirname(process.execPath) : userData` 作为数据目录，并做旧文件迁移；`store.js` 文件名维持 `spms-data.json`。
- 关于面板新增 `.about-scroll`（max-height + overflow-y auto）与 `.about-ver`（版本小标题）样式。

### 影响范围

- 出库表单交互与库位扫码为本次主要变更；入库保持 v2.4 行为并补充库位扫码按钮。
- 盘点仍保留「选择备品」下拉 + 扫码选备品（`scanForSelect` 已适配新扫码组件）。
- 历史数据完全兼容：`remark` 字段、`spms-data.json` 结构不变，仅存放位置变化。

---

## v2.4（2026-07-28）

### 变更（登记入库：扫码录入 + 库位反查 + 备注更名统一）

1. **「登记入库」备品选择由下拉框改为输入框，支持扫描枪直接扫码**
   - 原 `选择备品` 下拉框（在 600+ 备品时选择困难）改为 `备品编号` **输入框**，可直接用扫描枪扫码或手输编号（如 `SP20240101-001`）。
   - 输入/扫码后实时解析备品：显示备品名称，并把该备品的已知库位自动带入「库位号」输入框（可手动修改）。
   - 保留 📷 扫码识别按钮，识别后自动回填编号。

2. **新增「库位号」反向查询**
   - 在入库界面输入库位号（如 `A-01-03`），自动查找该库位对应的备品并**自动填写备品编号**。
   - 若同一库位存放多种备品，列出候选（编号 + 名称）供点选，避免歧义。

3. **全界面「备注」统一更名为「备注或用途」**
   - 覆盖：入库 / 出库 / 盘点 表单标签、入库 / 出库 / 记录查询 表格表头、CSV 导入模板与导出表头。
   - 历史 CSV（表头仍为「备注」）仍可正常导入：导入解析同时兼容 `备注` 与 `备注或用途` 两种表头。
   - 底层字段（`remark`）与存储不变，存量数据完全兼容。

### 实现要点

- 新增全局函数：`resolvePartByCode()`（编号→备品解析 + 正向联动库位）、`reverseLookupByLocation()`（库位→编号反查 + 多备品候选）、`pickReverseCode(code)`（候选点选回填）、`scanForCode(inputId)`（扫码回填编号）。
- `submitInbound` 改为按 `备品编号` 解析 `partId`，不再依赖下拉框的 `option.value`。
- `PART_HEADERS` / `PART_MAP` 的备注键改为 `备注或用途`；导入环节对旧表头 `备注` 做兼容映射。
- 新增样式：`.field-hint`（解析/反查提示，`ok`/`warn` 两态）、`.loc-cand`（反查候选标签）、`.about-sub`（历史版本小标题）。

### 影响范围

- 仅影响「登记入库」表单交互与「备注」字段的显示命名；出库 / 盘点仍保留原下拉选择（按需求本次仅改造入库）。
- 历史数据无迁移成本：`remark` 字段原样保留。

---

## v2.3（2026-07-27）

### 变更（分类 / 单位下拉框升级为「可持久化自定义」）

1. **自定义项加入选项列表并可持久化**
   - 选择「自定义…」后弹出一个输入框，确认后新分类 / 单位**自动加入下拉框的选项列表**，后续可直接选择该分类。
   - 已添加的自定义分类 / 单位通过 `DB.setMeta` 写入本地存储（Electron 为 `spms-data.json` 的 `meta` 字段；浏览器为 IndexedDB / localStorage），**刷新页面后依然保留，不会丢失**。
   - 启动时（`init`）从存储加载 `customCategories` / `customUnits` 到 `state`，下拉框自动包含历史自定义项。

2. **下拉框超出可视范围时自动滚动并限高**
   - 弹出选项列表设置 `max-height: 220px; overflow-y: auto`，选项过多时出现**垂直滚动条并限制最大显示高度**，不撑破布局、不影响其他元素。

3. **新增「关于 / About」按钮**
   - 固定展示在**主页右下角**，点击弹出面板，包含：
     - **变更日记**：记录本版本（2.3）主要更新内容。
     - **鸣谢**：感谢薛添才、朱近、林长平、林捷提供的宝贵意见。

### 实现要点

- 将原 `select + input + hidden` 三件套重写为**自定义组合下拉控件**：触发按钮 + 弹层（选项列表 + 自定义输入行 + “自定义…”入口）+ 隐藏字段（id 仍为 `f_category` / `f_unit`，`submitPart` 无需改动）。
- 新增函数：`comboToggle` / `comboSelect` / `closeAllCombos` / `comboOpenCustom` / `comboCancelCustom` / `comboAddCustom` / `renderComboList` / `positionComboPopup`；预设常量提升为模块级 `CATEGORY_PRESETS` / `UNIT_PRESETS` / `COMBO_META`。
- `comboAddCustom` 校验非空及去重（预设 + 已添加）后写入 `state` 并 `await DB.setMeta(...)` 持久化，再重渲染列表并选中新值。
- `positionComboPopup` 按视口可用空间自动决定向下 / 向上展开，避免被模态框裁切；全局点击空白处自动收起弹层。
- 编辑回显兼容：历史自定义值不在列表时临时补入选项，保证数据无损。

### 影响范围

- 仅影响「新增 / 编辑备品」表单与首页右下角按钮。
- 历史数据完全兼容：`customCategories` / `customUnits` 为空时行为与 2.2 一致；存量分类 / 单位照常显示。

---

## v2.2（2026-07-27）

### 变更（备品表单字段体验升级）

1. **「分类」改为下拉选择 + 自定义输入**
   - 原 `输入框（带 datalist 提示）` → `下拉框 + 自定义输入` 组合控件。
   - 预设选项：`电气`、`机械`、`液压`。
   - 选择「自定义…」后，下方自动出现输入框可填写任意自定义分类；保存时取输入框内容。
   - 编辑已有备品：若历史分类不在预设列表中，自动以「自定义…」模式打开并显示原值，保证数据无损回显。

2. **「单位」改为下拉选择 + 自定义输入**
   - 同样改为组合控件，预设选项：`个`、`件`、`米`。
   - 选择「自定义…」后显示输入框填写任意自定义单位；编辑回显逻辑同上。

3. **「备注」字段名改为「备注或用途」**
   - 仅修改表单 label 与占位语义，字段 id (`f_remark`) 与底层存储不变，存量数据完全兼容。

### 实现要点

- 新增 `comboHtml(name, currentValue, presets, fieldName)` 工具函数：渲染 `select + input + hidden` 三件套。
- 新增全局 `comboOnChange(name)` / `comboSync(name)`：下拉切换显示模式、自定义输入实时同步到隐藏字段。
- 提交逻辑 `submitPart` 无需改动（仍读 `#f_category` / `#f_unit` 的 `.value`，现在是 hidden 字段，由控件自动维护）。
- 样式沿用既有 `input, select, textarea` 统一样式，视觉与原页面布局保持一致。

### 影响范围

- 仅影响「新增 / 编辑备品」表单。
- 历史数据无迁移成本：`category` / `unit` 字段原样保留；老旧分类/单位仍可正常显示（自动进入「自定义」模式）。

---

## v2.1（2026-07-27）

### 新增
- **备品「随机生成编号」按钮**：在「新增备品 / 编辑备品」表单的「编号」字段旁新增「🎲 随机生成」按钮。点击后自动生成唯一备品编号并填入编号输入框，格式为 `SP + 年月日 + '-' + 三位序号`，例如 `SP20240101-001`：
  - 日期部分取当天 `YYYYMMDD`，序号部分基于当天已存在的同前缀编号自动递增，确保与现有备品不冲突（重复时继续自增，至多支持到 9999）。
  - 生成成功后自动选中填入的编号并提示「已生成编号：…」。
- 编号输入框占位符示例同步更新为 `如 SP20240101-001`。

### 优化
- 编号字段布局改为「输入框 + 生成按钮」内联排列（新增 `.input-with-btn` 样式）。

### 说明
- 编号字段本身一直存在且为必填项；本次主要补齐「一键生成唯一编号」能力，避免手工录入易重复/不规范。

---

## v2.0（历史版本）
- 全新 Electron 桌面壳，本地 JSON 文件存储（`%APPDATA%\备件仓库管理系统\spms\spms-data.json`），启动无需额外运行环境。
- 双模式数据层 `db.js`：Electron 走 IPC 落主进程 fs；浏览器直接打开 `index.html` 时回退 IndexedDB/localStorage。
- 备品 / 入库 / 出库 / 库存 / 库位 / 盘点 / 记录查询 七大模块 + 数据备份（CSV 导入导出、JSON 备份恢复）。
- 二维码能力：编码仅含 `SPMS1|<备品编号>`，扫描后按编号在主数据定位；支持批量打印标签与单张「下载 PNG / 打印」。
- 界面底部签名：`design by Frank, 2026.7.27`。
